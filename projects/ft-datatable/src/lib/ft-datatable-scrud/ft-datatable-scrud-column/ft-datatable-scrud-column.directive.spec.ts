import { FtDatatableScrudColumnDirective } from './ft-datatable-scrud-column.directive';

describe('FtDatatableScrudColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new FtDatatableScrudColumnDirective();
    expect(directive).toBeTruthy();
  });
});
