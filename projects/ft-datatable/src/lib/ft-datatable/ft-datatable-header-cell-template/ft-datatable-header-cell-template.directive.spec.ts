import { FtDatatableHeaderCellTemplateDirective } from './ft-datatable-header-cell-template.directive';

describe('FtDatatableHeaderCellTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new FtDatatableHeaderCellTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
